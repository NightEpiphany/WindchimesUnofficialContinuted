package com.moigferdsrte.windchimes.refine.dreamcatcher.component;

import net.minecraft.component.ComponentType;
import net.minecraft.registry.Registries;
import net.minecraft.registry.Registry;
import net.minecraft.util.Identifier;

import java.util.function.UnaryOperator;

public class DataInit {

//    public static final ComponentType<DreamcatcherComponent> DREAMCATCHER = register(
//            builder -> builder.codec(DreamcatcherComponent.CODEC).packetCodec(DreamcatcherComponent.PACKET_CODEC).cache()
//    );
//
//    private static <T> ComponentType<T> register(UnaryOperator<ComponentType.Builder<T>> builderOperator) {
//        return Registry.register(Registries.DATA_COMPONENT_TYPE, "dreamcatcher", ((ComponentType.Builder)builderOperator.apply(ComponentType.builder())).build());
//    }

    public static void init() {}
}
